---
name: research-notebook
description: >-
  生成便于逐步阅读、运行和核验的科研 Jupyter Notebook。用于根据论文、研究方案、
  数据说明或分析需求，从头编写数据分析、数值计算和科研复现代码；尤其适用于希望采用
  单文件线性流程、中文说明、适度抽象，并清楚记录方法、参数、数据处理和结果来源的任务。
  默认输出 Python .ipynb，用户明确要求时也可输出单文件 .py。
---

# 科研 Notebook

## 核心目标

生成一份研究者能够从头到尾阅读、逐单元运行、修改并核验的科研代码。保持研究依据、原始数据、处理步骤、参数和结果之间的清晰联系。

开始设计 Notebook 前，完整阅读 [references/notebook-guidelines.md](references/notebook-guidelines.md)。用户需要跨模型提示词时，再读取 [references/universal-prompt.md](references/universal-prompt.md)。

## 工作流程

### 1. 理解研究任务

- 阅读用户提供的论文、研究方案、公式、数据说明和预期输出。
- 列出研究目标、输入数据、核心方法、关键参数和需要生成的结果。
- 区分资料中明确给出的内容、用户设定、通用显示设置和待确认信息。
- 对会改变科学结论的缺失信息提出问题；获得依据后再实现相应计算。

### 2. 选择交付形式

- 中小型任务默认使用一个 `.ipynb` 文件。
- 用户明确要求脚本时，使用一个线性 `.py` 文件。
- 当运行规模、协作方式或外部接口确实需要拆分时，先说明原因，并保留清楚的主入口与数据流。
- 控制依赖数量，只引入完成研究所需的库。

### 3. 设计线性结构

- 按研究过程和结果形成顺序排列单元。
- 在首次使用之前定义变量和辅助函数。
- 为每个主要步骤写明研究依据、本步输入、执行内容、生成结果、质量检查和预计耗时。
- 在当前步骤就地展示数据样例、诊断、表格或图片。
- 将耗时计算拆成可观察的阶段，并显示必要的进度。

### 4. 编写易读代码

- 优先使用普通变量、列表、字典、DataFrame 和直观循环。
- 使用含义清楚的英文变量名，配合中文 Markdown 和中文注释。
- 注释重点解释数据含义、单位、处理原因、公式依据和结果解读。
- 为重复公式、重复读取或较复杂的独立计算编写小函数，并在函数前解释其研究含义。
- 仅在类、装饰器、类型体系、命令行入口、配置层或多文件结构能明显降低理解成本时引入这些形式。
- 让关键计算过程直接出现在 Notebook 中，便于研究者沿着数据流核验。

### 5. 保持科学溯源

- 从独立路径读取原始数据，将派生数据、缓存和结果写入明确的输出目录。
- 记录筛选、缺测处理、异常处理、单位转换、时间范围和空间范围。
- 在参数附近注明来源，可引用章节、公式、表格、文献、数据说明或用户设定。
- 将模型采用的常规默认值和计算假设显式列出。
- 使用已知研究结果进行核验，透明报告差异及可能原因。
- 保留完整结果，包括与预期不一致的结果；根据证据排查数据版本、单位、范围和实现细节。

### 6. 展示与保存结果

- 计算完成后立即显示关键输出，并保存需要交付的表格和图片。
- 集中设置字体、图幅、分辨率和输出格式，遵循用户给出的展示要求。
- 核对 Notebook 内嵌图片与导出文件的内容。内嵌后端表现不一致时，直接显示已保存的图片文件。
- 在末尾建立结果索引，关联结果名称、代码位置、输入、方法来源和输出文件。

### 7. 验证与交付

- 在条件允许时使用新内核从上到下运行 Notebook。
- 检查 JSON、Python 语法、路径、单位、缺失值、缓存来源和输出文件。
- 运行 `python scripts/validate_notebook.py <notebook.ipynb>`，处理错误并审阅警告。
- 说明已经实际运行的范围、尚未运行的重型步骤、外部依赖和已知差异。
- 只交付任务需要的 Notebook、结果和简短使用说明。

## 判断抽象是否合适

在增加结构前依次判断：

1. 这段逻辑是否重复出现？
2. 独立命名是否能说明研究含义？
3. 使用者是否更容易逐步调试？
4. 新结构是否隐藏了关键数据处理？

优先采用能够减少重复、提升理解且保持数据流可见的形式。

## 资源

- [references/notebook-guidelines.md](references/notebook-guidelines.md)：Notebook 结构、单元模板和质量检查清单。
- [references/universal-prompt.md](references/universal-prompt.md)：可交给不同模型使用的完整提示词和简短任务模板。
- `scripts/validate_notebook.py`：只读检查 Notebook 结构、语法、线性运行痕迹和可能增加阅读负担的代码形式。
